# Data. Real grids, availability exports, and filled schedules carry real RA
# names and never enter this repo, public or private.
*.xlsx
*.xls
*.csv
*.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# macOS
.DS_Store

# Reference material: prior conversations, screenshots, old scripts. Carries real
# names, so it stays local and never enters the repo. See CLAUDE.md.
references/
